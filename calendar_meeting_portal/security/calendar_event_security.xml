<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        
        <!-- Record Rule: Portal Users - Only Active Outcomes -->
        <record id="calendar_event_outcome_portal_active_rule" model="ir.rule">
            <field name="name">Portal Users: Only Active Outcomes</field>
            <field name="model_id" ref="model_calendar_event_outcome"/>
            <field name="domain_force">[('active', '=', True)]</field>
            <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <!-- Record Rule: Portal Employees - Can Update Own Meetings Outcome -->
        <record id="calendar_event_portal_attendee_rule" model="ir.rule">
            <field name="name">Portal Employees: Update Outcome if Attendee</field>
            <field name="model_id" ref="calendar.model_calendar_event"/>
            <field name="domain_force">[('partner_ids', 'in', user.partner_id.ids)]</field>
            <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="False"/>
        </record>

    </data>
</odoo>
